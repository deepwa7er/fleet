/target
# ide is a separate Cargo workspace with its own build dir (see ide/README.md).
/ide/target
node_modules/
.DS_Store

# Per-card git worktrees (`.agents/skills/fleet/SKILL.md` §2). Checkouts of this
# same repo, each with its own build dir — never content to commit.
/.worktrees/

# Per-card jj workspaces (fleet skill §2, post-cutover). Working copies of
# this same repo; a nested working copy must never be snapshotted or
# committed by the outer one.
/.workspaces/

# JetBrains project files — per-developer IDE state, created anywhere in the
# tree (the workspace root or any service dir), never part of the build.
.idea/

# The timeline generator output (timeline/build.mjs) — derived from the
# record repo, rebuilt on every publish, never content to commit.
/timeline/dist/
