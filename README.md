# fleet

Every Rust fleet service in one repo / one cargo workspace. Each top-level
directory was its own repository until 2026-07-01; full history was imported
with `git filter-repo --to-subdirectory-filter` (see
secondbrain/projects/fleet-monorepo.md for the migration plan). The old
repos are archived on GitHub.
