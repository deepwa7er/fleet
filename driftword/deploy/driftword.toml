# driftword service config.
#
# `bind` is loopback; the breakwater reverse proxy is the tailnet front door and
# serves driftword at https://driftword.intern.deepwa7er.net.
bind = "127.0.0.1"
port = 8091
